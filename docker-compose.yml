services:
  app:
    build: .
    secrets:
      - jwt_private_key
      - jwt_public_key
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
secrets:
  jwt_private_key:
    file: ./secrets/jwt-private-pkcs8.pem
  jwt_public_key:
    file: ./secrets/jwt-public.pem
