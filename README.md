# Personnel database for Studentradioen i Bergen (SRiB)

## Specifications

This project is using java version 21.

## Installing

- With docker nothing required, except latest version of docker :sunglasses: :moyai: 

## Running locally

## Running in docker compose

1. Go to the root folder of project

2. Run the following in terminal:

`sudo docker compose up --build -d`

3. Now you can go to `localhost:8080/swagger` to check if it runs

## Running in terminal

1. install linux

2. profit